[
  {
    "testCase": "Saldo insuficiente",
    "createTransfer":{
      "query": "mutation Mutation($from: String!, $to: String!, $amount: Float!) { transfer(from: $from, to: $to, amount: $amount) { from to amount date} }",
      "variables": {
        "from": "rodrigo",
        "to": "aline",
        "amount": 9999999
      }
    },
    "messageExpect": "Saldo insuficiente para realizar a transferência."
  },
  {
    "testCase": "Transferência para não favorefcido acima de 5k",
    "createTransfer": {
      "query": "mutation Mutation($from: String!, $to: String!, $amount: Float!) { transfer(from: $from, to: $to, amount: $amount) { from to amount date} }",
      "variables": {
        "from": "rodrigo",
        "to": "sheldon",
        "amount": 5000.01
      }
    },
    "messageExpect": "Transferências acima de R$ 5.000,00 só podem ser feitas para favorecidos do remetente."
  }
]